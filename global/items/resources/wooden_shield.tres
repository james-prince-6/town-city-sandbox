[gd_resource type="Resource" script_class="Item" load_steps=3 format=3]

[ext_resource type="Script" path="res://global/items/item.gd" id="1_item"]
[ext_resource type="PackedScene" path="res://assets/models/dev/prototype-kit/weapon-shield.fbx" id="2_model"]

[resource]
script = ExtResource("1_item")
id = &"wooden_shield"
display_name = "Wooden Shield"
description = "Hold block to raise it and soak a blow from the front — but every point of damage you block burns stamina, so it won't save you for long. Dodging is the real defence."
category = 6
max_stack = 1
base_value = 40
world_model = ExtResource("2_model")
block_modifier = 0.4
