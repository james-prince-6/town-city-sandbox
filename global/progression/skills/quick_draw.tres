[gd_resource type="Resource" script_class="Skill" load_steps=2 format=3]

; RANGED passive: shaves a fraction off ranged weapon cooldown. Progression clamps the
; total reduction so cooldown never drops below 30% of the weapon's base.

[ext_resource type="Script" path="res://global/progression/skill.gd" id="1_skill"]

[resource]
script = ExtResource("1_skill")
id = &"quick_draw"
display_name = "Quick Draw"
description = "Nock, draw, loose — all in one breath. -8% bow cooldown per rank."
branch = 1
max_rank = 4
cost = 1
required_level = 2
prerequisite = &""
prerequisite_rank = 1
is_perk = false
stat_per_rank = {
&"ranged_cooldown_reduction": 0.08
}
